import 'package:flutter/material.dart';

class AppTheme {
  AppTheme._();
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  // ADD primary color and secondary and other required color in theme in here as
  //  static const Color primaryColor = [PRIMARY COLOR]
  //  static const Color secondaryColor = [SECONDARY COLOR]
  // And use them wherever required as AppTheme.primaryColor or AppTheme.white so on...
}
